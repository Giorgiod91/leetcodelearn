var nums = [];

function removeDuplicate(nums) {
  var duplicate = [];
  for (let i = 0; i < nums.length; i++) {
    if (i[i] == i[i + 1]) {
      console.log(nums);
    }
  }
}

removeDuplicate([1, 1, 2]);
